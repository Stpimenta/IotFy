#ifndef ENUM_FUNCTIONS
#define ENUM_FUNCTIONS

enum enum_functions {
    TURN_ON_RELAY,
    TURN_OFF_RELAY,
    REVERSE_STATE,
    PULSE,
    RESTART_ESP,
    STATE,
    // UPDATE,
    NONE
}; 

#endif